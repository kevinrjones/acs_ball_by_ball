# API Server Configuration
API_PORT=8081

# Database Connection
DB_JDBC_URL=jdbc:mariadb://localhost:3306/acs_ball_by_ball
DB_USER=ballbyball
DB_PASSWORD=p4ssw0rd
DB_MAX_POOL_SIZE=10

# Database Host/Port (for reference / container environments)
DB_NAME=acs_ball_by_ball
DB_PORT=3306

# JWT / OIDC Configuration
JWT_JWKS_URL=https://ids.local:8443/.well-known/openid-configuration/jwks
JWT_ISSUER=https://ids.local:8443
JWT_REALM=Access to BallByBall API
JWT_AUDIENCE=bb.api
JWT_LEEWAY_SECONDS=3
