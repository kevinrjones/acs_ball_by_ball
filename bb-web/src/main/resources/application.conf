ktor {
    deployment {
        port = 8080
        port = ${?WEB_PORT}
    }
    application {
        modules = [ com.knowledgespike.ballbyball.web.ApplicationKt.module ]
    }
}

api {
    baseUrl = "http://localhost:8081"
    baseUrl = ${?API_BASE_URL}
}